/**
 * Simple placeholder for tabs not built yet (step 1 — Coach only).
 */
export function PlaceholderScreen({ title }) {
  return (
    <main className="dw-placeholder">
      <h1 className="dw-placeholder-title">{title}</h1>
      <p className="dw-placeholder-sub">Coming soon.</p>
    </main>
  );
}
